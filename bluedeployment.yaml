apiVersion: apps/v1
kind: Deployment
metadata:
  name: blue-deployment
  namespace: blue-green-deployment
  labels:
    app: example-app
    env: blue
spec:
  replicas: 3
  selector:
    matchLabels:
      app: example-app
      env: blue
  template:
    metadata:
      labels:
        app: example-app
        env: blue
    spec:
      containers:
      - name: example-container
        image: nginx:1.20-alpine
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"